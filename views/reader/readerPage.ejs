<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Reader Page</title>
    <style>
        /* Shared Styles */
        body {
            font-family: sans-serif;
            line-height: 1.8;
            margin: 0;
            color: #333;
            background: #fff;
            /* Flexbox added to body for sticky footer */
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        nav {
            background: #fff;
            padding: 15px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #eee;
        }

        .menu a {
            text-decoration: none;
            color: #333;
            margin-right: 20px;
            font-size: 14px;
        }

        .pub-btn {
            background: #007bff;
            color: white !important;
            padding: 8px 15px;
            border-radius: 4px;
            font-weight: bold;
        }

        /* Reader Content */
        .reader-container {
            max-width: 700px; 
            margin: 40px auto;
            padding: 0 20px;
            flex: 1; /* This pushes the footer down */
        }

        .back-link {
            display: inline-block;
            margin-bottom: 20px;
            color: #007bff;
            text-decoration: none;
            font-size: 14px;
        }

        .post-header h1 {
            font-size: 2.5rem;
            line-height: 1.2;
            margin-bottom: 10px;
        }

        .meta {
            color: #777;
            font-size: 0.9rem;
            margin-bottom: 30px;
            border-bottom: 1px solid #eee;
            padding-bottom: 20px;
        }

        .featured-image {
            width: 100%;
            height: 350px;
            background: #f0f0f0;
            border-radius: 8px;
            margin-bottom: 30px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #aaa;
            overflow: hidden;
        }

        .article-content p {
            margin-bottom: 20px;
            font-size: 1.1rem;
        }

        /* --- Footer Styles --- */
        footer {
            background: #f9f9f9;
            border-top: 1px solid #eee;
            padding: 40px 5% 20px;
            margin-top: 60px;
        }

        .footer-inner {
            max-width: 700px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
        }

        .footer-logo {
            font-weight: bold;
            color: #333;
        }

        .footer-links a {
            text-decoration: none;
            color: #666;
            font-size: 14px;
            margin-left: 20px;
        }

        .footer-links a:hover {
            color: #007bff;
        }

        .copyright {
            width: 100%;
            text-align: center;
            margin-top: 30px;
            font-size: 13px;
            color: #999;
        }

        /* Responsive */
        @media (max-width: 600px) {
            .post-header h1 { font-size: 1.8rem; }
            nav { flex-direction: column; gap: 10px; }
            .footer-inner { flex-direction: column; text-align: center; }
            .footer-links a { margin: 0 10px; }
        }
    </style>
</head>
<body>

    <nav>
        <div class="logo"><strong>MyBlog</strong></div>
        <div class="menu">
            <a href="/">Home</a>
            <a href="/login" target="_blank" class="pub-btn">Publisher</a>
        </div>
    </nav>
        
    <article class="reader-container">
         <a href="/" class="back-link">← Back to Home</a>
       
        <header class="post-header">
            <h1> <%= blog.title %></h1>
            <div class="meta">
                Published on <span><%= new Date(blog.createdAt).toDateString() %> </span> • By <strong>Admin</strong>
            </div>
        </header>

        <div class="featured-image">
            <img src="<%= blog.img %>" alt="<%= blog.title %>" style="width: 100%; height: 100%; object-fit: cover;">
        </div>

        <section class="article-content">
            <p><%= blog.content %></p>
        </section>
    </article>

    <footer>
        <div class="footer-inner">
            <div class="footer-logo">SimpleBlog</div>
            <div class="footer-links">
                <a href="/">Home</a>
                <a href="#">Privacy</a>
                <a href="#">Terms</a>
                <a href="#">About</a>
            </div>
        </div>
        <div class="copyright">
            &copy; 2026 SimpleBlog. All rights reserved.
        </div>
    </footer>

</body>
</html>