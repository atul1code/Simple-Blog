<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog- Home </title>
    <style>
        /* --- Modern CSS Variables --- */
        :root {
            --accent: #6366f1;
            --accent-hover: #4f46e5;
            --bg: #ffffff;
            --card-bg: #fdfdfd;
            --text-dark: #0f172a;
            --text-muted: #64748b;
            --nav-glass: rgba(255, 255, 255, 0.8);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Inter', -apple-system, sans-serif;
        }

        body {
            background-color: var(--bg);
            color: var(--text-dark);
            /* Added to ensure footer stays at bottom on short pages */
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* --- Navigation --- */
        header {
            position: sticky;
            top: 0;
            z-index: 100;
            background: var(--nav-glass);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid #e2e8f0;
            padding: 1rem 10%;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.4rem;
            font-weight: 800;
            letter-spacing: -0.5px;
            color: var(--text-dark);
        }

        nav {
            display: flex;
            align-items: center;
            gap: 2rem;
        }

        nav a {
            text-decoration: none;
            color: var(--text-muted);
            font-weight: 500;
            font-size: 0.95rem;
            transition: color 0.2s;
        }

        nav a:hover {
            color: var(--accent);
        }

        .publisher-btn {
            background: var(--accent);
            color: white !important;
            padding: 0.6rem 1.2rem;
            border-radius: 8px;
            font-weight: 600;
            transition: transform 0.2s, background 0.2s;
            box-shadow: 0 4px 6px -1px rgba(99, 102, 241, 0.3);
        }

        .publisher-btn:hover {
            background: var(--accent-hover);
            transform: translateY(-1px);
        }

        /* --- Hero Section --- */
        .hero {
            padding: 80px 10% 40px;
            max-width: 800px;
        }

        .hero h1 {
            font-size: 3rem;
            line-height: 1.1;
            margin-bottom: 1.5rem;
            color: var(--text-dark);
        }

        .hero p {
            font-size: 1.2rem;
            color: var(--text-muted);
        }

        /* --- Responsive Blog Grid --- */
        .blog-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
            gap: 2.5rem;
            padding: 40px 10%;
            margin-bottom: 60px; /* Adjusted margin */
            flex-grow: 1; /* Pushes footer down */
        }

        .post-card {
            background: var(--card-bg);
            border-radius: 16px;
            overflow: hidden;
            border: 1px solid #f1f5f9;
            transition: all 0.3s ease;
        }

        .post-card:hover {
            border-color: var(--accent);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.05);
        }

        .post-thumb {
            width: 100%;
            height: 220px;
            background: #f1f5f9;
            object-fit: cover;
        }

        .post-body {
            padding: 1.5rem;
        }

        .category {
            color: var(--accent);
            font-size: 0.75rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 0.5rem;
            display: block;
        }

        .post-body h2 {
            font-size: 1.25rem;
            margin-bottom: 0.75rem;
        }

        .post-body p {
            color: var(--text-muted);
            font-size: 0.95rem;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        /* --- Footer Styling --- */
        footer {
            background-color: #f8fafc;
            border-top: 1px solid #e2e8f0;
            padding: 4rem 10% 2rem;
            margin-top: auto;
        }

        .footer-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 2rem;
        }

        .footer-logo {
            font-weight: 800;
            color: var(--text-dark);
            font-size: 1.2rem;
        }

        .footer-links {
            display: flex;
            gap: 2rem;
        }

        .footer-links a {
            text-decoration: none;
            color: var(--text-muted);
            font-size: 0.9rem;
            transition: color 0.2s;
        }

        .footer-links a:hover {
            color: var(--accent);
        }

        .copyright {
            width: 100%;
            text-align: center;
            margin-top: 3rem;
            color: var(--text-muted);
            font-size: 0.85rem;
            border-top: 1px solid #e2e8f0;
            padding-top: 1.5rem;
        }

        /* --- Mobile Responsiveness --- */
        @media (max-width: 768px) {
            header { padding: 1rem 5%; }
            nav a:not(.publisher-btn) { display: none; }
            .hero { padding: 40px 5%; }
            .hero h1 { font-size: 2.2rem; }
            .blog-grid { padding: 20px 5%; }
            footer { padding: 3rem 5% 1.5rem; }
            .footer-content { flex-direction: column; text-align: center; }
            .footer-links { justify-content: center; }
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">SimpleBlog.</div>
        <nav>
            <a href="#">Stories</a>
            <a href="#">Archive</a>
            <a href="/login" class="publisher-btn" target="_blank" >Publisher Access</a>
        </nav>
    </header> 

    <section class="hero">
        <h1>Curated stories for the modern mind.</h1>
        <p>Explore the latest insights from industry leaders and creative thinkers around the globe.</p>
    </section>

    <main class="blog-grid">
        <% blogs.forEach(blog => { %>
        <div class="post-card">
        <div class="post-thumb" style="background: linear-gradient(to bottom right, #e0e7ff, #c7d2fe);">
            <img src="<%= blog.img %>" alt="<%= blog.title %>" style="width: 100%; height: 100%; object-fit: cover;">
        </div>
        <a href="/read/<%= blog._id %>" style="text-decoration: none;">
             <div class="post-body">
                <span class="category"> <%= blog.category %>  </span>
                <h2><%= blog.title %></h2>
                <p> <%= blog.content %> </p>
            </div>
           </a>
        </div>
     <% }) %>
    </main>

    <footer>
        <div class="footer-content">
            <div class="footer-logo">SimpleBlog.</div>
            <div class="footer-links">
                <a href="#">Privacy Policy</a>
                <a href="#">Terms of Service</a>
                <a href="#">Contact Us</a>
                <a href="#">About</a>
            </div>
        </div>
        <div class="copyright">
            &copy; 2026 SimpleBlog. All rights reserved.
        </div>
    </footer>

</body>
</html>